# [页面名称] PRD 片段模板（移动端）

## [序号]、[移动端子页面名称]

**页面**：[mobile-page-id]
**类型**：移动端
**菜单路径**：[上级页面] -> [当前页面]
**页面说明**：[说明该移动端子页面的业务用途]
**需求数**：[N]

### 页面功能描述

[说明移动端现场使用场景、操作人员、当前步骤要完成的业务目标。]

### 页面状态机

| 状态 | 业务含义 | 进入条件 | 可执行操作 | 后续状态 |
|------|----------|----------|------------|----------|
| 未开始 | 当前步骤未处理 | 进入页面 | 填写/打卡/选择 | 处理中 |
| 处理中 | 已部分填写 | 完成部分操作 | 暂存、继续、提交 | 已完成 |
| 已完成 | 当前步骤完成 | 满足完成条件 | 下一步、提交 | 下一页面 |

> 无独立状态机时写：无独立状态机，页面仅作为入口或只读展示。

### 页面特殊情况

| 场景 | 触发条件 | 处理规则 |
|------|----------|----------|
| 必填未完成 | 点击下一步或提交时缺少必填项 | 阻止继续并提示缺失内容 |
| 模拟扫码失败 | 用户触发异常场景 | 展示异常提示，不改变完成状态 |
| 照片不足 | 提交时照片数量不足 | 阻止提交并提示至少上传数量 |
| 返回上级 | 点击返回 | 保留或丢弃内容按业务规则说明 |

### [1] 页面入口/头部信息 {type=inline, target=mobile-header}

#### 功能说明

说明当前移动端页面的进入方式、返回目标和头部业务信息。

#### 展示字段

| 字段 | 类型 | 是否必填 | 默认值 | 长度/范围 | 校验规则 | 备注 |
|------|------|----------|--------|-----------|----------|------|
| 页面标题 | 文本展示 | 是 | [标题] | 无 | 无 | 标识当前步骤 |
| 返回目标 | 导航动作 | 是 | [上级页面] | 无 | 无 | 点击返回时进入目标页面 |

#### 操作规则

| 操作 | 触发位置 | 前置条件 | 成功结果 | 失败/异常处理 |
|------|----------|----------|----------|----------------|
| 返回 | 头部返回 | 无 | 返回上级页面 | 有未保存内容时按业务规则处理 |

### [2] 主业务内容 {type=inline, target=mobile-main}

#### 功能说明

说明当前子页面主体功能，例如选择点位、填写巡检情况、查看记录详情等。

#### 字段规则

| 字段 | 类型 | 是否必填 | 默认值 | 长度/范围 | 校验规则 | 备注 |
|------|------|----------|--------|-----------|----------|------|
| [字段] | 文本/下拉/单选/照片/扫码/签名 | 是/否/条件必填 | [默认值] | [限制] | [校验规则] | [业务说明] |

#### 操作规则

| 操作 | 触发位置 | 前置条件 | 成功结果 | 失败/异常处理 |
|------|----------|----------|----------|----------------|
| [操作] | [按钮/卡片/列表项] | [条件] | [结果] | [异常处理] |

#### 数据规则

| 数据项 | 规则 |
|--------|------|
| [数据项] | [选择、排序、保留、清空、自动生成等规则] |

### [3] 移动端采集能力 {type=inline, target=mobile-capture, scenarios=[预览异常:mock-error, 恢复正常:mock-success]}

#### 功能说明

用于描述拍照、扫码、签名、定位等移动端现场采集能力。原型阶段只做模拟，不接入真实设备能力。

#### 字段规则

| 字段 | 类型 | 是否必填 | 默认值 | 长度/范围 | 校验规则 | 备注 |
|------|------|----------|--------|-----------|----------|------|
| 照片 | 图片 | 条件必填 | 无 | 最少 [X] 张，最多 [Y] 张 | 数量不足时不可提交 | 原型阶段使用模拟图片 |
| 扫码结果 | 文本展示 | 条件必填 | 无 | 无 | 需与当前业务对象匹配 | 原型阶段模拟成功/失败 |
| 签名 | 图片 | 条件必填 | 无 | 1 张 | 未签名不可提交 | 原型阶段模拟手写签名 |

#### 操作规则

| 操作 | 触发位置 | 前置条件 | 成功结果 | 失败/异常处理 |
|------|----------|----------|----------|----------------|
| 拍照 | 拍照按钮 | 当前步骤允许拍照 | 新增一张模拟照片 | 超过上限时提示 |
| 扫码 | 扫码按钮 | 当前步骤允许扫码 | 生成模拟核对结果 | 不匹配时提示异常，不完成步骤 |
| 签名 | 签名区域 | 当前步骤需要签名 | 保存模拟签名 | 未确认时不保存 |

### [4] 底部操作/步骤流转 {type=inline, target=mobile-actions}

#### 功能说明

说明当前移动端子页面的暂存、下一步、提交、完成等操作。

#### 操作规则

| 操作 | 触发位置 | 前置条件 | 成功结果 | 失败/异常处理 |
|------|----------|----------|----------|----------------|
| 暂存 | 底部操作区 | 无 | 保存当前进度并保留在当前/返回列表 | 暂存失败时提示 |
| 下一步 | 底部操作区 | 当前步骤必填完整 | 进入下一子页面 | 必填缺失时阻止 |
| 提交 | 底部操作区 | 全部步骤满足完成条件 | 生成记录并跳转目标页面 | 校验失败时提示 |

#### 状态变化

| 当前状态 | 操作 | 条件 | 下一状态 |
|----------|------|------|----------|
| 处理中 | 暂存 | 任意进度 | 未提交 |
| 处理中 | 提交 | 全部必填完成 | 已提交 / 已完成 |

### [5] 结果/确认弹窗 {type=modal, modalId=resultModal, trigger=req:4.action:submit}

#### 触发规则

| 触发入口 | 前置条件 | 打开内容 |
|----------|----------|----------|
| 提交 | 提交完成 | 展示提交结果、异常数量或下一步提示 |

#### 展示内容

| 场景 | 展示内容 | 后续动作 |
|------|----------|----------|
| 全部正常 | 成功提示 | 跳转记录列表或详情 |
| 存在异常 | 异常数量和整改提示 | 跳转记录详情或列表 |
| 提交失败 | 失败原因 | 停留当前页面继续处理 |
