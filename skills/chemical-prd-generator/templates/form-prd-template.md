# [N] [页面名称] PRD (产品需求文档)

> 需求编号 [N] 对应页面标注中的角标编号，与 `annotation-panel` 联动使用。

## 1. 引言

### 1.1 背景
> 简要说明该表单页面的核心定位、解决的问题、用户价值（控制在 50 字左右）。

```
[需求背景内容]
```

### 1.2 功能清单
> 以表格形式梳理所有模块、功能点及功能描述。

| 模块 | 功能点 | 功能描述 |
|------|--------|---------|
| `[功能模块标题]` | `[功能点标题]` | `[功能点描述]` |

## 2. 页面菜单与权限规划

### 2.1 页面菜单
> 仅描述菜单路径，不做其他内容说明。

**菜单路径**：[一级菜单名称] / [二级菜单名称]

### 2.2 权限规划
> 查看类操作归集在【查看】；编辑类操作归集在【编辑】。

| 权限点 | 操作范围 | 数据范围 |
|--------|---------|---------|
| `[权限点名称]` | `[支持的操作名称]` | `[个人/全量]` |

## 3. [N] 基本信息区 — 需求说明

> 需求编号 [N] 对应页面中基础信息表单区域的角标。

### 3.1 表单字段

| 字段名称 | 控件类型 | 输入限制 | 必填 | 默认值 |
|:---------|:---------|:---------|:----|:------|
| `[字段名称]` | `[文本输入/下拉选择/日期选择/单选/多选/上传]` | `[长度/格式/范围]` | 是/否 | `[默认值/无]` |

### 3.2 字段校验规则

| 校验项 | 校验规则 | 触发时机 | 错误提示 |
|--------|---------|---------|---------|
| `[字段名称]` | `[校验规则描述]` | `[失焦/提交]` | `[错误提示文案]` |

## 4. [N] 业务信息区 — 需求说明

> 需求编号 [N] 对应页面中业务信息表单区域的角标。若页面有多个业务区块（如证照信息、化学品信息），各自独立编号。

### 4.1 表单字段

| 字段名称 | 控件类型 | 输入限制 | 必填 | 默认值 |
|:---------|:---------|:---------|:----|:------|
| `[字段名称]` | `[控件类型]` | `[输入限制]` | 是/否 | `[默认值]` |

### 4.2 字段联动规则

- `[字段A]` 选择 `[值X]` 时，`[字段B]` 显示/隐藏/变更可选值
- `[字段C]` 的值需大于 `[字段D]`

## 5. [N] 提交流程 — 需求说明

> 需求编号 [N] 对应页面底部操作按钮区域的角标。

### 5.1 操作按钮

| 按钮 | 行为 | 前置条件 | 后置结果 |
|------|------|---------|---------|
| **提交** | 校验全部必填字段，提交数据 | 所有必填项已填写，校验通过 | 数据保存成功，跳转回列表页 |
| **保存草稿** | 跳过必填校验，保存当前已填数据 | 无 | 保存草稿状态，停留在当前页 |
| **取消** | 返回上一页 | 无 | 二次确认是否放弃已填内容 |

### 5.2 提交后业务流转

> 描述提交成功后触发的业务动作：

- 数据进入 `[状态名称]` 状态
- 通知 `[角色名称]` 进行下一步处理
- 生成 `[关联单据/记录]`

## 6. 交互设计

### 6.1 全局交互

- **加载状态**：打开编辑页时，若为编辑已有数据，需展示 Loading 状态直到数据回填完成。
- **表单保护**：用户已填写内容但未保存，离开页面时弹出二次确认"是否放弃已填写内容？"
- **提交反馈**：提交成功后展示成功提示；失败时展示错误原因，不关闭页面。

### 6.2 特殊交互

- **字段联动**：[描述字段间的显示/隐藏/校验联动规则]
- **证照校验**：[描述证照编号的格式校验规则]
- **附件上传**：[描述上传的文件类型、大小限制、数量限制]
