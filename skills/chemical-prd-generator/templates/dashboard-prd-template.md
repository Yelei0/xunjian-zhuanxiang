# [N] [页面名称] PRD (产品需求文档)

> 需求编号 [N] 对应页面标注中的角标编号，与 `annotation-panel` 联动使用。

## 1. 引言

### 1.1 背景
> 简要说明该看板的核心定位、解决的问题、用户价值（控制在 50 字左右）。

```
[需求背景内容]
```

### 1.2 功能清单
> 以表格形式梳理所有模块、功能点及功能描述。

| 模块 | 功能点 | 功能描述 |
|------|--------|---------|
| `[功能模块标题]` | `[功能点标题]` | `[功能点描述]` |

## 2. 页面菜单与权限规划

### 2.1 页面菜单
> 仅描述菜单路径，不做其他内容说明。

**菜单路径**：[一级菜单名称] / [二级菜单名称]

### 2.2 权限规划
> 查看类操作归集在【查看】；编辑类操作归集在【编辑】；导出类操作归集在【导出】。

| 权限点 | 操作范围 | 数据范围 |
|--------|---------|---------|
| `[权限点名称]` | `[支持的操作名称]` | `[个人/全量]` |

## 3. [N] 数据概览区 — 需求说明

> 需求编号 [N] 对应页面中指标卡片区域的角标。

### 3.1 指标卡片

| 指标名称 | 业务定义 | 统计口径 | 更新频率 |
|:---------|:---------|:---------|:---------|
| `[指标名称]` | `[指标的業務含义]` | `[统计规则]` | `[实时/每小时/每日]` |

- **交互说明**：
  - 点击指标卡片，可下钻至对应的详情列表页。
  - 指标数据异常时（低于/高于阈值），卡片展示告警状态。

### 3.2 阈值告警

> 描述指标的告警阈值及触发规则。

| 指标 | 阈值条件 | 告警级别 | 处理指引 |
|------|---------|---------|---------|
| `[指标名称]` | `[阈值判断条件]` | `[黄色预警/红色告警]` | `[对应操作指引]` |

## 4. [N] 图表区 — 需求说明

> 需求编号 [N] 对应页面中图表区域的角标。

### 4.1 图表类型与内容

| 图表 | 类型 | 展示内容 | 联动交互 |
|------|------|---------|---------|
| `[图表名称]` | `[柱状图/折线图/饼图/地图]` | `[展示的数据维度]` | `[与其他图表的联动关系]` |

### 4.2 时间范围切换

- 支持切换时间范围：今日 / 近7天 / 近30天 / 自定义日期
- 切换时间范围后，所有图表和指标同步刷新

## 5. [N] 实时状态区 — 需求说明

> 需求编号 [N] 对应页面中状态列表或滚动日志区域的角标。

### 5.1 状态列表

| 列表字段 | 字段说明 |
|:---------|:---------|
| `[字段名称]` | `[字段业务定义]` |

- **刷新机制**：数据每 [X] 秒自动刷新 / 手动点击刷新按钮。

### 5.2 状态标签

| 状态名称 | 业务含义 | 触发条件 |
|---------|---------|---------|
| `[状态名称]` | `[状态对应的业务含义]` | `[什么情况下显示该状态]` |

## 6. 交互设计

### 6.1 全局交互

- **加载状态**：数据请求过程中各图表区域独立展示 Loading 骨架屏。
- **空状态**：无数据时展示 Empty 状态，并提示"暂无数据，请调整筛选条件"。
- **异常状态**：单个图表数据加载失败时不阻塞其他区域，失败区域展示重试按钮。

### 6.2 特殊交互

- **图表联动**：[描述图表之间的联动筛选关系]
- **下钻跳转**：[描述从看板跳转到详情列表的规则]
- **自动刷新**：[描述数据自动刷新的策略和开关]
