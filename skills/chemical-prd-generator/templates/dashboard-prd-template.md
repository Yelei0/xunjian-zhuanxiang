# [页面名称] PRD 片段模板（数据看板）

## [序号]、[页面名称]

**页面**：[page-id]
**类型**：数据看板
**菜单路径**：[一级菜单] / [二级菜单]
**页面说明**：[说明该看板监控的业务对象和决策价值]
**需求数**：[N]

### 页面功能描述

[说明看板用于监控什么业务、发现什么问题、支撑什么处置动作。不要写图表样式实现。]

### 页面状态机

| 状态 | 业务含义 | 进入条件 | 可执行操作 | 后续状态 |
|------|----------|----------|------------|----------|
| 正常 | 指标处于正常范围 | 未触发阈值 | 查看明细 | 预警 / 告警 |
| 预警 | 指标达到预警阈值 | 达到黄色阈值 | 查看明细、处理 | 正常 / 告警 |
| 告警 | 指标达到危险阈值 | 达到红色阈值 | 查看明细、处理 | 正常 |

> 无业务状态时写：无独立状态机，看板仅展示统计结果。

### 页面特殊情况

| 场景 | 触发条件 | 处理规则 |
|------|----------|----------|
| 单个图表无数据 | 当前统计口径下无数据 | 图表区域展示空状态，不影响其他区域 |
| 指标加载失败 | 模拟加载失败 | 指标区域展示失败提示和重试入口 |
| 自动刷新关闭 | 用户关闭刷新 | 数据保持当前快照，允许手动刷新 |

### [1] 时间范围/筛选条件 {type=inline, target=dashboard-filter}

#### 功能说明

系统支持按时间范围或业务维度切换看板统计口径，切换后所有指标和图表同步刷新。

#### 字段规则

| 字段 | 类型 | 是否必填 | 默认值 | 长度/范围 | 校验规则 | 备注 |
|------|------|----------|--------|-----------|----------|------|
| 时间范围 | 单选/日期范围 | 是 | 今日 | 今日、近7天、近30天、自定义 | 自定义结束时间不得早于开始时间 | 影响全部统计模块 |

#### 操作规则

| 操作 | 触发位置 | 前置条件 | 成功结果 | 失败/异常处理 |
|------|----------|----------|----------|----------------|
| 切换时间 | 筛选区 | 时间范围合法 | 指标和图表同步刷新 | 时间不合法时提示 |
| 手动刷新 | 筛选区 | 无 | 重新生成模拟数据 | 刷新失败时保留原数据 |

### [2] 核心指标 {type=inline, target=metric-cards}

#### 功能说明

系统展示当前统计口径下的核心业务指标，用于快速识别整体运行状态。

#### 指标口径

| 指标 | 业务含义 | 统计口径 | 默认值 | 告警规则 | 点击行为 |
|------|----------|----------|--------|----------|----------|
| [指标] | [含义] | [计算规则] | [默认值] | [阈值] | [下钻或无] |

#### 数据规则

| 数据项 | 规则 |
|--------|------|
| 环比/同比 | 原型阶段使用模拟值，需说明正负含义 |
| 异常指标 | 达到阈值时以告警语义展示，并进入告警列表 |

### [3] 图表分析 {type=inline, target=charts}

#### 功能说明

系统通过趋势、分布、排行等图表展示业务变化和异常集中区域。

#### 图表口径

| 图表 | 图表目的 | 数据维度 | 统计口径 | 联动规则 |
|------|----------|----------|----------|----------|
| [图表] | [目的] | [维度] | [计算规则] | [点击、筛选、下钻] |

#### 操作规则

| 操作 | 触发位置 | 前置条件 | 成功结果 | 失败/异常处理 |
|------|----------|----------|----------|----------------|
| 图表点击 | 图表项 | 当前项存在明细 | 联动筛选或下钻详情 | 无明细时提示暂无数据 |

### [4] 实时状态/告警列表 {type=inline, target=status-list}

#### 功能说明

系统展示当前需要关注的状态、事件或告警，辅助用户快速定位待处理事项。

#### 列表字段

| 字段 | 字段说明 | 是否必显 | 展示规则 |
|------|----------|----------|----------|
| [字段] | [业务含义] | 是/否 | [格式、枚举、颜色语义] |

#### 状态规则

| 状态 | 业务含义 | 触发条件 | 处理规则 |
|------|----------|----------|----------|
| 正常 | 无异常 | 未达到阈值 | 仅展示 |
| 预警 | 需要关注 | 达到预警阈值 | 可查看详情 |
| 告警 | 需要处理 | 达到危险阈值 | 可进入处理入口 |

### [5] 告警详情弹窗 {type=modal, modalId=alertDetailModal, trigger=req:4.action:view-alert}

#### 触发规则

| 触发入口 | 前置条件 | 打开内容 |
|----------|----------|----------|
| 查看告警 | 告警记录存在 | 打开告警详情 |

#### 展示内容

| 区域 | 内容 | 规则 |
|------|------|------|
| 基础信息 | 告警对象、级别、时间 | 只读展示 |
| 触发原因 | 阈值、异常描述 | 说明判断依据 |
| 处理建议 | 处置指引 | 原型阶段仅展示 |
