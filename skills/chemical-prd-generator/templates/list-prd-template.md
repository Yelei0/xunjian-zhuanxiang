# [N] [页面名称] PRD (产品需求文档)

> 需求编号 [N] 对应页面标注中的角标编号，与 `annotation-panel` 联动使用。

## 1. 引言

### 1.1 背景
> 简要说明该页面的核心定位、解决的问题、用户价值（控制在 50 字左右）。

```
[需求背景内容]
```

### 1.2 功能清单
> 以表格形式梳理所有模块、功能点及功能描述。不包含页面顶栏、左侧菜单栏的样式、交互需求内容。

| 模块 | 功能点 | 功能描述 |
|------|--------|---------|
| `[功能模块标题]` | `[功能点标题]` | `[功能点描述]` |

## 2. 页面菜单与权限规划

### 2.1 页面菜单
> 仅描述菜单路径，不做其他内容说明。

**菜单路径**：[一级菜单名称] / [二级菜单名称]

### 2.2 权限规划
> 查看类操作归集在【查看】；编辑类操作归集在【编辑】；导出类操作归集在【导出】。不定义具体角色。

| 权限点 | 操作范围 | 数据范围 |
|--------|---------|---------|
| `[权限点名称]` | `[支持的操作名称]` | `[个人/全量]` |

## 3. [N] 筛选查询区 — 需求说明

> 需求编号 [N] 对应页面中筛选区的角标。

### 3.1 筛选字段

支持多维度组合筛选，筛选条件变更后需点击"查询"按钮生效。

| 筛选字段 | 类型 | 输入限制 | 匹配规则 |
|:---------|:-----|:---------|:---------|
| `[筛选字段名称]` | `[文本输入/下拉选择/下拉多选/日期范围选择器]` | `[长度限制/枚举值/默认值/必填性/条件依赖]` | `[精准/模糊/范围]` |

- **操作按钮**：
  - **查询**：根据当前筛选条件刷新列表数据。
  - **重置**：恢复所有筛选条件为默认值。

## 4. [N] 数据列表 — 需求说明

> 需求编号 [N] 对应页面中数据表格区域的角标。

### 4.1 列表字段

| 列表字段 | 字段说明 | 排序 |
|:---------|:---------|:----|
| `[字段名称]` | `[字段业务定义]` | 是/否 |

### 4.2 分页规则

- 每页默认显示：[X] 条
- 可切换显示：[X/X/X] 条
- 分页控件位置：列表底部

### 4.3 批量操作

- **批量删除**：选中多条记录后点击删除，弹出二次确认弹窗。
- **批量导出**：选中多条记录后点击导出，导出选中数据（CSV 格式）。

### 4.4 行操作

- **编辑**：点击编辑按钮，跳转至编辑页面。
- **删除**：点击删除按钮，弹出二次确认弹窗。
- **查看详情**：点击查看按钮，跳转至详情页面。

### 4.5 交互说明

- **排序**：点击表头排序图标，支持升序/降序切换。
- **导出**：点击导出按钮，导出当前筛选条件下的全部数据（CSV 格式）。

## 5. 交互设计

### 5.1 全局交互

- **加载状态**：数据请求过程中需展示 Loading 状态。
- **空状态**：无数据时需展示统一的 Empty 状态图，并提示"暂无数据"。
- **异常状态**：请求失败时展示错误提示及重试入口。

### 5.2 特殊交互

> 描述页面中特殊的业务交互逻辑：

- **筛选条件联动**：[描述筛选字段之间的联动关系]
- **数据刷新机制**：[描述数据何时自动刷新、手动刷新方式]
- **弹窗交互**：[描述弹窗的触发条件、内容、确认/取消行为]
